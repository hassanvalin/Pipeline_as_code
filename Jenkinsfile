def name = "Hello"

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Hello 6"
                echo "The name is: ${name}"
            }
        }
    }
}
